name: CI

on:
  push:
    branches: [main, release, develop]
  pull_request:
    branches: [main, release, develop]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
          run_install: false

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Run tests
        run: pnpm test --watch=false

      - name: Build
        run: pnpm build

  e2e-smoke:
    runs-on: ubuntu-latest
    needs: quality

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
          run_install: false

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Build SSR bundle
        run: pnpm build

      - name: Run SSR smoke E2E
        env:
          CYPRESS_BASE_URL: http://127.0.0.1:4500
        run: |
          pnpm start:ssr:e2e > /tmp/ssr.log 2>&1 &
          SERVER_PID=$!
          trap 'kill $SERVER_PID || true; cat /tmp/ssr.log' EXIT
          for i in {1..60}; do
            if curl -sSf "$CYPRESS_BASE_URL" > /dev/null; then
              break
            fi
            sleep 1
          done
          curl -sSf "$CYPRESS_BASE_URL" > /dev/null
          pnpm e2e
